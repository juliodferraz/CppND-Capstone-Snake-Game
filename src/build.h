#ifndef BUILD_H
#define BUILD_H
/**
 *  File used for setting some macros for SnakeGame build configuration.
 */

/**
 *  \brief Indicates if game is running on debug mode (which activates command line prints and the snake world view perspective rendering), or not.
 */
#define DEBUG_MODE 0

#endif